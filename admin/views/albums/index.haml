%ul.nav.nav-tabs
  %li.active=link_to tag_icon(:list, pat(:list)), url(:albums, :index)
  %li=link_to tag_icon(:plus, pat(:new)), url(:albums, :new)

.tabs-content
  %table#list.table.table-striped.table-hover.table-condensed
    %thead
      %tr
        %th.header.list-menu
          .dropdown
            %a.list-menu-toggle.dropdown-toggle{:href => '#', :'data-toggle' => :dropdown}
              =tag_icon :magic
              %span.caret
            %ul.list-menu.dropdown-menu{:role => :menu}
              %li.list-menu-wrapper=link_to tag_icon(:check, pat(:select_all)), '#', :id => 'select-all', :class => 'list-menu-link'
              %li.list-menu-wrapper.list-menu-wrapper-disabled
                =link_to tag_icon(:'check-empty', pat(:deselect_all)), '#', :id => 'deselect-all', :class => 'list-menu-link list-menu-link-disabled'
              %li.list-menu-divider.divider
              %li.list-menu-wrapper.list-menu-wrapper-disabled
                =link_to tag_icon(:trash, pat(:delete_selected)), '#', :id => 'delete-selected', :class => 'list-menu-link list-menu-link-disabled'
                .list-menu-popover-delete-selected.popover.right
                  .arrow
                  %h3.popover-title=pat(:delete_selected_title)
                  .popover-content
                    = form_tag url(:albums, :destroy_many), :method => :delete do
                      =hidden_field_tag :album_ids, :'data-delete-many-ids' => true
                      =submit_tag pat(:delete), :class =>'list-menu-popover-delete-selected-btn btn btn-danger btn-small'
                      .btn.btn-default.btn-small.cancel=pat(:cancel)
        %th.header= mat(:album, :id)
        %th.header= mat(:album, :created_at)
        %th.header= mat(:album, :updated_at)
        %th.header= mat(:album, :title)
        %th.header= mat(:album, :release_date)
        %th.header= mat(:album, :cover_url)
        %th.header= mat(:album, :label)
        %th.header= mat(:album, :type)
        %th.header= mat(:album, :bandcamp_url)
        %th.header= mat(:album, :bandcamp_id)
        %th.header.list-row-action-header

    %tbody
      -@albums.each do |album|
        %tr.list-row
          %td.list-column.list-selectable
            =check_box_tag 'album_ids[]', :value => album.id, :class => 'list-selectable-checkbox'
          %td.list-column=album.id
          %td.list-column=album.created_at
          %td.list-column=album.updated_at
          %td.list-column=album.title
          %td.list-column=album.release_date
          %td.list-column=album.cover_url
          %td.list-column=album.label
          %td.list-column=album.type
          %td.list-column=album.bandcamp_url
          %td.list-column=album.bandcamp_id
          %td.list-column.list-row-action
            .list-row-action-wrapper
              =link_to tag_icon(:edit), :href => url(:albums, :edit, :id => album.id), :rel => :tooltip, :title => "#{pat(:edit)} album", :class => 'list-row-action-wrapper-link'
              =link_to tag_icon(:trash), :rel => :tooltip, :title => "#{pat(:delete)} album", :class => 'list-row-action-delete-one list-row-action-wrapper-link'
              .popover.list-row-action-popover-delete-one.left
                .arrow
                %h3.popover-title=pat(:delete, :model => "album")
                .popover-content
                  = form_tag url(:albums, :destroy, :id => album.id), :method => :delete do
                    =submit_tag pat(:delete), :class =>'list-row-action-popover-delete-one-btn btn btn-danger btn-small'
                    .btn.btn-default.btn-small.cancel=pat(:cancel)
